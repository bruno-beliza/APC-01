#include <stdio.h>

int main()
{
    float notas[10];

    float soma;

    float media;

    int qtde_acima_media;

    int posicao;

    for(int i=0; i<10; i++)
    {
        printf("Informe uma posicao de nota: %i", posicao);
        scanf("%i", &posicao);
    }

    return 0;
}